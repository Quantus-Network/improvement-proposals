<pre>
  QIP: 13
  Title: Poseidon2 Proof of Work  
  Author: Chris Smith <chris@quantus.com>
  Comments-Summary: No comments yet.
  Comments-URI: https://github.com/Quantus-Network/improvement-proposals/discussions/
  Status: Draft
  Type: Standards
  Created: 2025-10-25
</pre>

## Abstract

This document describes a proof of work algorithm that mimics the desirable properties of Bitcoin's consensus algorithm, while offering a significant advantage to anyone wishing to prove block correctness in a ZK setting.

## Motivation

Cryptographic hash functions are weakened, but not destroyed by quantum computers. In fact, such hash functions are used as core primitives in the design of post-quantum-cryptographic algorithms, such as [SPHINCS+](https://sphincs.org/).

Using Grover's algorithm to reverse such hash functions is very impractical, [as it requires hundreds of thousands of gates and thousands of logical qubits](https://arxiv.org/abs/1603.09383) and only provides a quadratic speedup over classical methods. Therefore, we do not expect Grover's algorithm to be used to mine Bitcoin anytime soon.

However, SHA256 is notoriously inefficient when using a proving system like Plonk or STARKs. Therefore we use a newer hash function [Poseidon2](https://eprint.iacr.org/2023/323.pdf) which is optimized for ZK and therefore gives us a huge reduction in the number of gates and a corresponding speedup in proving time.

With this blackbox construction, miners grind on nonces to find a "small enough" digest, where "small enough" is a dynamic difficulty threshold that retargets every block using an Ethereum-style adjustment, targeting a configured block time (starting at 12 seconds).

Overall this construction is meant to closely imitate Bitcoin's consensus algorithm with the main changes being the hash function and per-block difficulty updates.

## Specification

We expose a function `is_valid_nonce` that checks a `block_hash` and nonce against a given difficulty. For Poseidon2 we use sponge width 12, rate 8, and capacity 4 (≈256 bits of classical capacity security). We squeeze the sponge twice to obtain a 512-bit digest, hence the `U512` usage.

```
pub fn is_valid_nonce(block_hash: [u8; 32], nonce: [u8; 64], difficulty: U512) -> (bool, U512) {
	if difficulty == U512::zero() {
		log::error!(
			"is_valid_nonce should not be called with 0 difficulty, but was for block_hash: {:?}",
			block_hash
		);
		return (false, U512::zero());
	}

	let hash_result = get_nonce_hash(block_hash, nonce);
	log::debug!(target: "math", "hash_result = {:x}, difficulty = {:x}",
		hash_result.low_u64(), difficulty.low_u64());

	// In Bitcoin-style PoW, we check if hash < target
	// Where target = max_target / difficulty
	let max_target = U512::MAX;
	// Unchecked division because we catch difficulty == 0 above
	let target = max_target / difficulty;

	(hash_result < target, hash_result)
}

// Single Poseidon2 absorb of (block_hash || nonce), then squeeze twice for 512 bits
pub fn get_nonce_hash(
	block_hash: [u8; 32], // 256-bit block_hash
	nonce: [u8; 64],      // 512-bit nonce
) -> U512 {
	let mut input = [0u8; 96]; // 32 + 64 bytes
	input[..32].copy_from_slice(&block_hash);
	input[32..96].copy_from_slice(&nonce);

	let hash = qp_poseidon_core::hash_squeeze_twice(&input);
	U512::from_big_endian(&hash)
}
```

### Difficulty adjustment

Difficulty is recalculated every block with the Ethereum PoW-style formula:

```text
diff' = parent_diff + (parent_diff / 2048) * max(1 - block_time / divisor, -99)
```

where `divisor = target_block_time * 2 / 3` (8s for a 12s target). This yields:

- block time &lt; divisor: difficulty increases by ~1/2048
- divisor to 2·divisor: unchanged
- slower blocks: difficulty decreases by up to 99/2048 per block

Results are clipped to configured min/max difficulty bounds. Observed block time used for retargeting is floored to a small minimum so implausibly small timestamp deltas cannot steer the adjustment.

## Reference Implementation

PoW math lives in [qpow-math](https://github.com/Quantus-Network/chain/tree/main/qpow-math) and the runtime pallet in [pallets/qpow](https://github.com/Quantus-Network/chain/tree/main/pallets/qpow).

Poseidon2 comes from [qp-poseidon](https://github.com/Quantus-Network/qp-poseidon) (`SPONGE_WIDTH = 12`, `SPONGE_RATE = 8`, `SPONGE_CAPACITY = 4`).

## Rationale

Poseidon2 offers up to a 70% reduction in gates compared to Poseidon1, which itself offers a 95% gate reduction compared to SHA256.

Bitcoin's infrequent difficulty updates (~every 2015 blocks) produce a chunky difficulty function. Per-block Ethereum-style adjustment (±1/2048 steps) responds quickly to hash-rate changes while bounding how far difficulty can move in a single block.

We use a single Poseidon2 absorb of `(block_hash || nonce)` with a double squeeze for 512-bit output, rather than hashing the digest a second time. The rate/capacity choice (8/4) matches the production sponge parameters in `qp-poseidon`.

It is worth noting that Poseidon2 outputs field elements and we use the Goldilocks field, which has order 2^64 - 2^32 + 1 == 0xffffffff00000001. This means the output of the function is not uniformly distributed across the byte space, but instead is uniformly distributed across byte representations of the field elements. We consider this acceptable as we are "hashing towards zero" and the exclusion of large values should not affect the ability of the network to mine.

## Copyright

This specification is released into the public domain.
